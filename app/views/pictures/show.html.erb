<p id="notice"><%= notice %></p>

<%= image_tag @picture.image.url %>

<p>
  <strong>Name:</strong>
  <%= @picture.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @picture.description %>
</p>

<%= link_to 'Edit', edit_picture_path(@picture), class:"btn btn-link" %> |
<%= link_to 'Back', pictures_path, class:"btn btn-link" %>
